# Copy to .env.local for local overrides
# Leave unset to use same-origin (works with Vite proxy + ngrok on port 5173)
# VITE_SERVER_URL=http://localhost:4000

# For a separate ngrok tunnel to the server (port 4000), set:
# VITE_SERVER_URL=https://your-server-tunnel.ngrok-free.dev
